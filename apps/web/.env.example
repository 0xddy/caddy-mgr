# Nest API 仅监听本机，由 Nuxt 将同源 /api 请求代理到这里。
NUXT_API_BASE_URL=http://127.0.0.1:3001

# Nitro Node 监听配置（生产环境由 PM2 注入）。
NITRO_HOST=0.0.0.0
NITRO_PORT=3000

# 两项同时设置时启用 Nitro HTTPS。
# NITRO_SSL_CERT=/etc/caddy-mgr/tls/fullchain.pem
# NITRO_SSL_KEY=/etc/caddy-mgr/tls/privkey.pem
